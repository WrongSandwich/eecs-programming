#include <ctime>  //CLOCKS_PER_SEC

clock_t start, end, diff;
double runtime;
start = clock();
sortA(array, a_size);
end = clock();
diff = end - start; //Clock ticks consumed by sort algorithm, need to convert to seconds
runtime = diff/CLOCKS_PER_SEC;
